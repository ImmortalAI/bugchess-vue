export type UserData = {
  id: string;
  email: string;
  username: string;
  rating: number;
  sigma: number;
};

export type UserSearchResult = {
  id: string;
  username: string;
  rating: number;
};
